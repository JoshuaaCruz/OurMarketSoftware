/**
 * Interface unificada para todos os menus da aplicação
 * Define o contrato comum a qualquer tipo de menu (produtos, categorias, etc.)
 */
package view;

/**
 * Unified interface for all application menus
 */
public interface Menu_if {
    /**
     * Displays the menu to the user and handles interaction
     */
    void mostre();
}
