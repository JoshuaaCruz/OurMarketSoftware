{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Run src Main",
      "request": "launch",
      "mainClass": "Main",
      "cwd": "${workspaceFolder}",
      "classPaths": [
        "${workspaceFolder}/out",
        "${workspaceFolder}/src"
      ],
      "args": "--terminal"
    }
  ]
}
