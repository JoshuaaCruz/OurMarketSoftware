{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile src",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-Command",
        "New-Item -ItemType Directory -Force out | Out-Null; $files = Get-ChildItem -Recurse -Filter *.java src | ForEach-Object { $_.FullName }; javac -encoding UTF-8 -d out $files"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$javac"
    }
  ]
}
